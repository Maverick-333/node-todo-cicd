version: 0.2

env:
  parameter-store:
    DOCKER_USERNAME: "/myapp/docker-credentials/username"
    DOCKER_PASSWORD: "/myapp/docker-credentials/password"
    DOCKER_REGISTRY_URL: "/myapp/docker-registry/url"

phases:
  install:
    runtime-versions:
      nodejs: 12

  pre_build:
    commands:
      - echo Logging in to DockerHub...
      - docker login -u "$DOCKER_USERNAME" --password "$DOCKER_PASSWORD"
      - IMAGE_REPO_NAME="test-node-app"
      - IMAGE_NAME="$DOCKER_USERNAME/$IMAGE_REPO_NAME"
  
  build:
    commands:
      - echo "Building Docker image..."
      - docker build -t $IMAGE_NAME:latest .

  post_build:
    commands:
      - echo "Pushing Docker image to DockerHub..."
      - docker push $IMAGE_NAME:latest
      - echo "Uploading zip file to S3..."
      - aws s3 cp node-todo-app.zip s3://new-test-bucket-122/node-todo-app/
      - echo "Writing image definition file..."
      - printf '{"ImageURI":"%s", "Port":8000}' $IMAGE_NAME:latest > imageDefinitions.json
      - cat imageDefinitions.json

artifacts:
  files:
    - '**/*'
  base-directory: '.'
  discard-paths: no
  exclude-patterns:
    - 'node_modules/**/*'
    - '.git/**/*'
    - '.gitignore'
    - 'README.md'
    - 'package-lock.json'
